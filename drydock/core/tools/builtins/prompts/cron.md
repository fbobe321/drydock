Schedule recurring prompts.

- `cron_create(prompt="Check git status", interval_minutes=30)` — Run every 30 min
- `cron_list()` — Show all scheduled crons
- `cron_delete(cron_id="abc123")` — Remove a cron
