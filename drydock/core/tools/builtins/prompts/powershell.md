Run PowerShell commands (Windows or pwsh on Linux/macOS).

- `powershell(command="Get-Process | Select-Object -First 5")`
- `powershell(command="Get-ChildItem -Recurse -Filter *.py")`

Similar to bash but uses PowerShell syntax. On Linux/macOS requires pwsh.
